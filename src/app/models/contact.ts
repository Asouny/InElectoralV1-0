import { NumericValueAccessor } from '@ionic/angular';

export class Contact {
    id?: string;
    affair: string;
    email: string;
    firstname: string;
    lastname: string;
    message: string;
    phone: number;
}
